# 仿禅道 PMS 系统 · 全功能测试手册

> 用途：按模块逐项点击验证系统全部功能，帮助你掌握整个 PMS 的能力边界与操作路径。
> 适用环境：本机已启动前端（:5173）与后端（:8081），数据库 `pms` 已初始化种子数据。

---

## 0. 环境准备

| 项目 | 地址 / 命令 |
|------|------------|
| 前端入口 | http://localhost:5173/ |
| 后端 API | http://localhost:8081/api （Spring Boot 3） |
| 数据库 | MySQL 8，`pms`，root / 666666 |
| 登录接口 | `POST /api/auth/login` → 返回 JWT（同时写入 Redis 会话） |
| 登录态判断 | 后端全局异常返回 HTTP 200 + `Result(code≠200)`；前端按 `res.code` 判断成败 |

> ⚠️ **验收纪律**：浏览器验收时先用目标账号登录并做完所有操作；**不要再用同一账号（尤其 admin）跑 curl 登录**，否则会互踢浏览器里的 token 导致自动跳登录页。冒烟测试请改用 `dev01` 等其它账号。

### 测试账号（密码统一 `123456`）

| 账号 | 角色 | 可见菜单（动态菜单，按权限下发） |
|------|------|-------------------------------|
| `admin` | 系统管理员 | **全部模块**（推荐用它做全功能验收） |
| `po01` | 产品 | 工作台、产品列表、需求管理、发布管理、统计、全局搜索 |
| `pm01` | 项目 | 工作台、项目列表/详情/看板、统计、全局搜索 |
| `dev01` | 开发 | 工作台、迭代看板、统计、全局搜索（+ 任务相关操作） |
| `qa01` | 测试 | 工作台、Bug管理、测试用例、测试单、统计、全局搜索 |

> 说明：左侧菜单来自后端 `userStore.menus`，随角色权限变化。**用 `admin` 登录能看到并操作所有菜单与按钮**（按钮受 `v-perm` 指令控制，已修复为响应式显示）。用角色账号可顺带验证「无权限按钮不显示」的权限隔离效果。

---

## 1. 登录与全局导航

1. 打开 http://localhost:5173/ → 未登录自动跳 `/login`。
2. 输入 `admin` / `123456` → 登录 → 跳转 `/dashboard`（工作台）。
3. 顶部布局：左侧为动态菜单（按角色），顶部右上角有 **全局搜索框** 与 **用户下拉**（退出登录）。
4. 全局搜索框（新增功能）：输入关键词（如 `登录`）→ 回车 → 跳转 `/search?keyword=登录`，分「需求 / 任务 / Bug」三类展示结果。

---

## 2. 工作台 `/dashboard`

验证入口：登录后默认页。

- **统计卡片**：产品数、需求数、项目数、Bug 数等概览（由 `dashboard/summary` 驱动）。
- **我的待办任务**：`dashboard/my-tasks` 返回的当前用户任务列表（表格）。
- **待我处理的 Bug**：`dashboard/my-bugs` 返回的当前用户待处理 Bug 列表（表格）。

✅ 预期：3 个区域均有数据；点击列表项可跳转到对应详情页。

---

## 3. 产品管理

### 3.1 产品列表 `/product/list`

1. 菜单「产品管理 → 产品列表」。
2. **新增产品**：点「新增」→ 填名称/代号/描述 → 保存 → 列表新增一行。
3. **编辑**：行内「编辑」→ 改字段 → 保存。
4. **删除**：行内「删除」→ 确认 → 行消失。
5. **分页/搜索**：顶部关键字 + 分页控件正常。

✅ 预期：增删改查闭环；`GET /products/page` 驱动列表。

### 3.2 需求管理 `/product/story` ⭐含批量操作

1. 菜单「产品管理 → 需求管理」。
2. **提需求**：点「提需求」(`story:add`) → 选产品、填标题/优先级/预估工时/状态(draft) → 保存 → 列表 +1。
3. **状态流转**：行内「流转」→ 选动作（如 评审通过 / 关闭）→ 状态变更（受状态机校验）。
4. **指派**：行内「指派」→ 选人员 → 保存。
5. **⭐ 批量选择**：勾选左侧选择框多选若干行 → 工具栏出现「批量删除」「批量指派」。
6. **⭐ 批量删除**：点「批量删除」→ 二次确认弹窗 → 确认 → 选中行被删除（`POST /stories/batch-delete`）。
7. **⭐ 批量指派**：点「批量指派」→ 弹窗选人员 → 确认 → 选中行 assignedTo 统一更新（`POST /stories/batch-assign`）。
8. **查看**：点击某需求进入详情（含操作日志时间线、评论）。

✅ 预期：单条与批量操作均生效；批量按钮在权限加载完成后渲染（已修复 `v-perm` 异步问题）。

### 3.3 发布管理 `/product/release`

1. 菜单「产品管理 → 发布管理」。
2. 新增发布：选产品、填版本号/日期/说明 → 保存。
3. 编辑 / 删除 / 列表筛选。

---

## 4. 项目管理

### 4.1 项目列表 `/project/list`

1. 菜单「项目管理 → 项目列表」。
2. 新增项目：填名称/代号/起止日期/状态 → 保存。
3. 编辑 / 删除 / 分页。

### 4.2 项目详情 `/project/detail/:id`

1. 列表中点击某项目 → 进入详情。
2. **成员管理**：查看成员列表；「添加成员」→ 选用户 + 角色 → 保存；行内「移除成员」。
3. **状态流转**：项目状态机动作（开始/完成/关闭）。
4. 需求/任务关联区（若有）。

✅ 预期：`GET /projects/{id}/members`、`POST /projects/{id}/members` 生效。

### 4.3 迭代看板 `/project/board` ⭐拖拽

1. 菜单「项目管理 → 迭代看板」。
2. 选择「项目」→ 选择「迭代（Sprint）」→ 看板按「待办/进行中/已完成」分列加载任务。
3. **拖拽流转**：用鼠标把某任务卡片从「待办」列拖到「进行中」列 → 状态更新（`PUT /tasks/{id}/move`）。
   - 注：看板用原生 HTML5 拖拽（Drag & Drop），真人鼠标拖拽生效；自动化工具（Playwright 模拟鼠标拖拽）不触发，验收请手动拖。
4. 卡片上可直接「指派」「记录工时」「流转状态」。

✅ 预期：拖拽后列间卡片数量变化且接口返回成功。

---

## 5. 质量中心

### 5.1 Bug 管理 `/qa/bug` ⭐含批量操作

1. 菜单「质量中心 → Bug 管理」。
2. 新建 Bug：选产品/项目、填标题/严重程度/优先级/复现步骤 → 保存。
3. 状态流转：行内「流转」→ 选动作（激活/已解决/关闭）+ 解决方案 → 状态变更。
4. 指派：行内「指派」→ 选人。
5. **⭐ 批量删除 / 批量指派**：与需求页一致（勾选 → 工具栏按钮 → 二次确认/弹窗选人），对应 `POST /bugs/batch-delete`、`POST /bugs/batch-assign`。

### 5.2 测试用例 `/qa/case`

1. 菜单「质量中心 → 测试用例」。
2. 新增用例：填标题/前置条件/步骤/预期结果/所属模块 → 保存。
3. 编辑 / 删除 / 列表。

### 5.3 测试单 `/qa/suite`

1. 菜单「质量中心 → 测试单」。
2. 新增测试单：填名称、关联用例、执行人、计划时间 → 保存。
3. **执行测试单**：行内「执行」(`PUT /test-suites/{id}/run`) → 录入各用例结果 → 提交。
4. 编辑 / 删除。

---

## 6. 统计报表 `/stats` ⭐含工时汇总图

1. 菜单「统计报表」。
2. **选择维度**：先选「项目」，再选「迭代（Sprint）」。
3. 自动加载四张图（均由 `GET /stats/*` 与 `GET /tasks/workhour-summary` 驱动）：
   - **燃尽图** `stats/burndown`：剩余工时随时间下降曲线。
   - **任务分布** `stats/task-distribution`：各状态任务数量。
   - **Bug 分布** `stats/bug-distribution`：按严重程度/状态分布（按产品维度）。
   - **⭐ 工时汇总** `tasks/workhour-summary`：三维柱状图（预估 / 已消耗 / 剩余）。**必须选中迭代后才会出现数据**（之前验证：选迭代后 canvas 真实渲染、非透明像素达数万）。
4. 切换不同项目/迭代，图表随之刷新。

✅ 预期：4 张 ECharts 图均有数据；不报错；切换维度即时刷新。

---

## 7. 全局搜索 `/search`

> 入口有两种：
> - **顶部搜索框**（推荐）：输入关键词回车 → 直接跳 `/search?keyword=xxx`。
> - 手动访问 `/search` 并输入关键词。

结果按类型分块展示：**需求 / 任务 / Bug**。验证：搜 `登录` 应返回需求 1 条 + 任务 2 条（种子数据）。点击结果可跳转详情。

✅ 预期：跨模块模糊匹配；分类计数正确；点击可跳转。

---

## 8. 系统管理（建议用 `admin`）

### 8.1 用户管理 `/system/user`

1. 新增用户：用户名/姓名/角色/部门/密码 → 保存（`POST /users`）。
2. 编辑、禁用/启用（「修改状态」`PUT /users/{id}/status`）、删除。
3. 列表与分页。

### 8.2 角色管理 `/system/role`

1. 新增角色：名称/编码/描述 → 保存。
2. 给角色分配权限（关联权限树）。
3. 编辑 / 删除。

### 8.3 权限管理 `/system/perm`

1. 权限树展示（`GET /perms/tree`）。
2. 新增权限节点：父级/名称/编码/类型/路由 → 保存。
3. 编辑 / 删除。

### 8.4 部门管理 `/system/dept`

1. 部门树（`GET /depts/tree`）。
2. 新增子部门：名称/负责人/排序 → 保存。
3. 编辑 / 删除（删除有子节点时通常受限）。

### 8.5 操作日志 `/system/log`

1. 日志列表（`GET /logs/page`）：操作人/模块/动作/时间/IP。
2. 可按条件筛选；时间线视图（`GET /logs/timeline`）。
3. 详情查看（部分支持评论/删除附件）。

✅ 预期：此前各模块的增删改操作都会在此留下记录（操作日志切面已修复，使用 Spring 注入的 ObjectMapper 序列化日期）。

---

## 9. 新增功能专项验收（#24 收官）

| 功能 | 入口 | 验证步骤 | 预期 |
|------|------|----------|------|
| 全局搜索框 | 顶部 Header | 输入 `登录` → 回车 | 跳转 `/search`，需求 1 + 任务 2 |
| 工时汇总图 | 统计报表 | 选 项目 → 选 迭代 | 三维柱状图渲染（预估/已耗/剩余） |
| 需求批量删除 | 需求管理 | 勾选多行 → 批量删除 → 确认 | 选中行消失 |
| 需求批量指派 | 需求管理 | 勾选多行 → 批量指派 → 选人 | 选中行 assignedTo 更新 |
| Bug 批量删除/指派 | Bug 管理 | 同上 | 同上 |

> 修复记录：① 搜索框与批量按钮曾因改错布局文件（`Layout.vue` 而非 `index.vue`）不显示，已修正；② `v-perm` 指令原为 `mounted` 时 `removeChild` 不可逆删除按钮，已改为「响应式 `display` 切换 + `watch(perms)`」，异步权限加载后按钮正常出现。

---

## 10. 验收检查表（Checklist）

复制以下清单逐项打勾：

```
□ 登录 admin，默认进入工作台
□ 工作台：统计卡片 / 我的任务 / 待处理 Bug 均有数据
□ 产品列表：增 / 改 / 删 / 分页
□ 需求管理：提需求 / 状态流转 / 单条指派
□ 需求管理：批量删除（二次确认）
□ 需求管理：批量指派（弹窗选人）
□ 发布管理：增 / 改 / 删
□ 项目列表：增 / 改 / 删
□ 项目详情：成员增 / 删 / 状态流转
□ 迭代看板：选迭代 + 拖拽卡片改状态
□ Bug 管理：新建 / 流转 / 单条指派
□ Bug 管理：批量删除 / 批量指派
□ 测试用例：增 / 改 / 删
□ 测试单：增 / 执行(run)
□ 统计报表：选项目+迭代 → 燃尽图/任务分布/Bug分布/工时汇总 四图
□ 全局搜索：顶部框输入回车 → /search 分类结果
□ 用户管理：增 / 启停 / 删
□ 角色管理：增 / 配权限
□ 权限管理：树 + 增 / 删
□ 部门管理：树 + 增 / 删
□ 操作日志：列表 / 筛选 / 时间线
□ 权限隔离：用 po01/pm01/dev01/qa01 登录，确认菜单与按钮按角色收敛
□ 退出登录：右上角下拉 → 退出 → 回 /login
```

---

## 附：常用后端接口速查（context-path `/api`）

| 域 | 关键接口 |
|----|----------|
| 认证 | `POST /auth/login`、`GET /auth/info` |
| 产品 | `GET /products/page`、`POST /products`、`PUT /products/{id}`、`DELETE /products/{id}` |
| 需求 | `GET /stories/page`、`POST /stories`、`PUT /stories/{id}/status`、`PUT /stories/{id}/assign`、`POST /stories/batch-delete`、`POST /stories/batch-assign` |
| 发布 | `GET /releases/page`、`POST /releases` … |
| 项目 | `GET /projects/page`、`POST /projects`、`GET /projects/{id}/members`、`POST /projects/{id}/members` |
| 迭代 | `GET /sprints`、`POST /sprints`、`GET /sprints/{id}/stories`、`POST /sprints/{id}/stories` |
| 任务 | `GET /tasks/board`、`PUT /tasks/{id}/move`、`PUT /tasks/{id}/hours`、`GET /tasks/workhour-summary` |
| Bug | `GET /bugs/page`、`POST /bugs`、`PUT /bugs/{id}/status`、`POST /bugs/batch-delete`、`POST /bugs/batch-assign` |
| 测试用例 | `GET /testcases/page`、`POST /testcases` … |
| 测试单 | `GET /test-suites/page`、`POST /test-suites`、`PUT /test-suites/{id}/run` |
| 统计 | `GET /stats/burndown`、`GET /stats/task-distribution`、`GET /stats/bug-distribution` |
| 搜索 | `GET /search?keyword=` |
| 系统 | `GET /users/page`、`PUT /users/{id}/status`、`GET /roles`、`GET /perms/tree`、`GET /depts/tree`、`GET /logs/page` |
| 工作台 | `GET /dashboard/summary`、`GET /dashboard/my-tasks`、`GET /dashboard/my-bugs` |
