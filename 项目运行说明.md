# 仿禅道敏捷项目管理系统 - 运行说明

## 1. 项目简介

参考 [ZenTao PMS](https://github.com/easysoft/zentaopms) 的核心业务骨架，使用 **Spring Boot 3 + Vue 3 + MySQL 8 + Redis** 重新实现的敏捷项目管理系统。

核心闭环：**产品 → 需求 → 项目 → 迭代 → 任务 → Bug → 发布**

---

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Spring Boot 3.4 + MyBatis-Plus 3.5.7 + Spring Security + JWT + Redis |
| 前端 | Vue 3 + Vite 5 + Element Plus + Pinia + Axios + ECharts |
| 数据库 | MySQL 8（账号 `root` / `666666`，数据库 `pms`） |
| 缓存 | Redis 7（默认端口 6379，db0） |

---

## 3. 目录结构

```
E:\work\毕业设计3
├── backend/                 # Spring Boot 后端工程
│   ├── sql/schema.sql       # 建库脚本 + 初始数据
│   └── src/main/java/com/pms
├── frontend/                # Vue3 前端工程
│   ├── src/views/           # 页面
│   └── src/api/             # API 封装
└── 禅道仿做-系统设计方案.md  # 系统设计文档
```

---

## 4. 数据库初始化

首次运行前执行：

```bash
mysql -uroot -p666666 --default-character-set=utf8mb4 < "E:/work/毕业设计3/backend/sql/schema.sql"
```

脚本已包含：
- 约 30 张核心表（RBAC 五件套 + 产品/需求/项目/迭代/任务/Bug/发布/测试用例/燃尽图等）
- 初始权限菜单树
- 5 个默认账号（密码均为 `123456`）

---

## 5. 启动方式

### 5.1 启动 Redis

确保本地 Redis 在 `127.0.0.1:6379` 运行：

```bash
redis-server
redis-cli ping   # 应返回 PONG
```

### 5.2 启动后端

```bash
cd "E:/work/毕业设计3/backend"
MH="C:\Users\asus\.m2\apache-maven-3.9.9"
java -classpath "$MH\boot\plexus-classworlds-2.8.0.jar" \
  "-Dclassworlds.conf=$MH\bin\m2.conf" \
  "-Dmaven.home=$MH" \
  "-Dmaven.multiModuleProjectDirectory=E:\work\毕业设计3\backend" \
  org.codehaus.plexus.classworlds.launcher.Launcher \
  spring-boot:run -DskipTests
```

- 服务地址：`http://localhost:8081`
- API 前缀：`/api`
- Swagger UI：`http://localhost:8081/api/swagger-ui.html`

### 5.3 启动前端

```bash
cd "E:/work/毕业设计3/frontend"
"C:\Users\asus\.workbuddy\binaries\node\versions\22.22.2\npm.cmd" run dev
```

- 访问地址：`http://localhost:5173`
- Vite 代理：`/api` → `http://localhost:8081`

---

## 6. 默认账号

| 账号 | 姓名 | 角色 | 用途 |
|---|---|---|---|
| `admin` | 系统管理员 | ADMIN | 全部权限 |
| `po01` | 陈产品 | 产品经理 | 管理产品与需求 |
| `pm01` | 王项目 | 项目经理 | 管理项目、迭代、成员 |
| `dev01` | 李开发 | 开发工程师 | 处理任务 |
| `qa01` | 赵测试 | 测试工程师 | 提交 Bug、写用例 |

密码均为：`123456`

---

## 7. 核心功能演示路径

| 功能 | 页面路径 | 说明 |
|---|---|---|
| 登录 | `/login` | admin/123456 |
| 工作台 | `/dashboard` | 统计卡片 + 燃尽图 + Bug 分布 + 待办 |
| 产品列表 | `/product/list` | 演示产品 DEMO |
| 需求管理 | `/product/story` | 用户登录功能（P1/已激活） |
| 发布管理 | `/product/release` | 商城一期 v1.0.0 |
| 项目列表 | `/project/list` | 商城一期（进行中） |
| 项目详情 | `/project/detail/1` | 迭代 Sprint 1 + 项目成员 + 需求 |
| 迭代看板 | `/project/board?projectId=1&sprintId=1` | 四列任务看板 |
| Bug 管理 | `/qa/bug` | 密码错误提示不友好 |
| 测试用例 | `/qa/case` | 空状态可新增 |
| 统计报表 | `/stats` | 燃尽图 + 任务分布 + Bug 分布 |
| 用户管理 | `/system/user` | 5 个系统账号 |
| 角色管理 | `/system/role` | ADMIN/PO/PM/DEV/QA |
| 权限管理 | `/system/perm` | 树形菜单与按钮权限 |
| 部门管理 | `/system/dept` | 研发/产品/测试部 |
| 操作日志 | `/system/log` | AOP 记录的操作流水 |

---

## 8. 答辩演示脚本（建议 3 分钟）

1. **登录**：使用 `admin` 进入系统，展示工作台。
2. **工作台**：讲解四个统计卡片 + 迭代燃尽图 + Bug 分布图。
3. **产品/需求**：进入产品列表 → 需求管理，说明需求池。
4. **项目/迭代**：进入项目详情，展示 Sprint 1、成员、拉入的需求。
5. **任务看板**：进入 `/project/board?projectId=1&sprintId=1`，拖动任务状态（可选）。
6. **质量闭环**：进入 Bug 管理，说明任务完成后的测试与缺陷跟踪。
7. **发布**：进入发布管理，说明版本发布聚合了本次完成的需求与修复的 Bug。
8. **系统管理**：简要展示 RBAC（用户/角色/权限/部门）。

---

## 9. 常见问题

**Q: 登录后很快跳回登录页？**
A: 同一个账号在浏览器外使用 curl/API 登录会覆盖 Redis 里的 token，导致浏览器端 token 失效。重新登录即可。

**Q: 看板页空白？**
A: 看板需要先选择项目和迭代，或直接访问带参数的 URL：`/project/board?projectId=1&sprintId=1`。

**Q: 燃尽图没有数据？**
A: 燃尽图依赖 `sprint_burndown` 表的每日快照，由后端定时任务生成。演示数据已为 Sprint 1 插入 5 天快照。

**Q: 后端报 `object_id` 相关错误？**
A: 已修复。所有 `@LogOperation(objectId = "#result")` 已改为 `#result.data`，确保返回 `Result<Long>` 时正确取到 ID。

---

## 10. 后端关键配置

文件：`backend/src/main/resources/application.yml`

- 服务端口：`8081`
- 上下文路径：`/api`
- MySQL：`jdbc:mysql://localhost:3306/pms?...`
- JWT 有效期：12 小时
