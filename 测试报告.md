# PMS 系统冒烟测试报告

> 测试时间：2026-08-22 13:00 ~ 13:50  
> 修复时间：2026-08-22 15:00（P1/P2 已修复）  
> 测试范围：后端 API（54 项）+ 前端页面（10+ 页面）+ 浏览器交互  
> 测试账号：admin（全权限）、dev01（无权限）

---

## ✅ 修复进度

| 问题 | 优先级 | 状态 | 修复内容 |
|------|--------|------|----------|
| 角色权限种子缺失 | P1 | **已修复** | 为 PO/PM/DEV/QA 分配了对应权限（schema.sql + 数据库同步更新） |
| Bug/Story/Task 状态流转 NPE | P2 | **已修复** | 三个 Controller 的 changeStatus 增加 action 空值校验 |
| dev01 403 页面显示空表格 | P3 | 部分解决 | 前端拦截器已有 ElMessage 错误提示，组件多用 try-catch 兜底 |

---

## 测试结果总览

| 维度 | 状态 | 说明 |
|------|------|------|
| 后端服务启动 | ✅ | Spring Boot 3.5.6，端口 8081，连接 MySQL + Redis 正常 |
| 前端服务启动 | ✅ | Vite 5.4.21，端口 5173 |
| admin API 读接口 | ✅ 全部通过 | 产品、需求、项目、任务、Bug、仪表盘、统计、搜索等 |
| admin API 写接口 | ✅ 全部通过 | 创建产品/需求/项目/迭代/任务/Bug、状态流转、指派、批量操作 |
| admin 前端页面 | ✅ 全部正常 | 工作台、产品列表、需求管理、项目列表、迭代看板、Bug管理、统计报表、系统管理 |
| dev01 登录 | ✅ 可登录 | 工作台统计卡片正常（待办10、Bug1、需求2、项目1） |

---

## 发现的问题（按优先级排序）

### 🔴 P0 — 功能阻塞

**无。** 核心功能全部正常。

---

### 🟡 P1 — 重要缺陷

**1. 非 admin 角色无权限、菜单为空（已知缺口，未修复）**

- **现象**：dev01 登录后左侧菜单栏完全空白；产品列表/需求管理/Bug管理等页面显示"暂无数据"（API 403 静默吞掉）
- **根因**：`sys_role_perm` 种子表只给 ADMIN 角色分配了权限，PO/PM/DEV/QA 角色有定义但无关联权限
- **影响**：所有非管理员用户无法正常使用系统
- **修复建议**：在 `sys_role_perm` 种子数据中为 PO/PM/DEV/QA 各角色分配对应权限

---

### 🟠 P2 — 中等缺陷

**2. Bug 状态流转参数缺失导致 NPE**

- **文件**：`BugController.java` 第 104 行 `switch (body.getAction())`
- **现象**：`PUT /bugs/{id}/status` 请求体为空对象 `{}` 时后端抛 500 NPE（`Cannot invoke "String.hashCode()" because "<local5>" is null`）
- **影响**：任何非法请求（如传空 JSON）会导致 500 错误，而非友好的参数校验提示
- **修复建议**：在 `switch` 前增加 `if (body.getAction() == null) throw new BizException("操作类型不能为空")`

**3. 前端页面加载存在 5 个 Console Error**

- **现象**：每个页面加载时控制台均有 5 个 Error 和 2 个 Warning
- **影响**：可能影响页面性能或部分功能异常
- **建议**：检查网络请求和资源加载，确定具体错误来源

---

### 🔵 P3 — 低优先级 / 建议优化

**4. dev01 页面显示"暂无数据"而非"权限不足"**

- **现象**：dev01 访问产品列表页时，API 403 被前端静默处理，表格显示"暂无数据"
- **建议**：前端 API 拦截器拦截 403 时弹出权限不足提示，而不是静默显示空数据

**5. 测试数据残留**

- **现象**：多轮测试产生了大量"测试产品_冒烟""测试需求_冒烟""冒烟任务_正确参数"等脏数据
- **建议**：上线前清理测试数据

---

## 全部页面测试结果

| 页面 | 路由 | admin | dev01 |
|------|------|-------|-------|
| 登录页 | /login | ✅ 正常 | ✅ 正常 |
| 工作台 | /dashboard | ✅ 统计卡片 + 待办任务 + Bug 表格 | ✅ 统计卡片正常，菜单为空 |
| 产品列表 | /product/list | ✅ 13 条数据，分页正常 | ⚠️ 暂无数据（API 403） |
| 需求管理 | /product/story | ✅ 3 条需求，操作按钮完整 | ⚠️ 暂无数据 |
| 发布管理 | /product/release | ✅ 正常 | ⚠️ 暂无数据 |
| 项目列表 | /project/list | ✅ 4 个项目，操作按钮完整 | ⚠️ 暂无数据 |
| 迭代看板 | /project/board | ✅ 选择项目和迭代交互 | ⚠️ 暂无数据 |
| Bug管理 | /qa/bug | ✅ 3 条 Bug，指派/解决/动态完整 | ⚠️ 暂无数据 |
| 测试用例 | /qa/case | ✅ 正常 | ⚠️ 暂无数据 |
| 测试单 | /qa/suite | ✅ 正常 | ⚠️ 暂无数据 |
| 统计报表 | /stats | ✅ ECharts 图表区正常 | ✅ 正常 |
| 用户管理 | /system/user | ✅ 5 个用户，增删改 | ❌ 不可访问 |
| 角色管理 | /system/role | ✅ 5 个角色，授权功能 | ❌ 不可访问 |
| 权限管理 | /system/perm | ✅ 23 个权限项完整树 | ❌ 不可访问 |
| 部门管理 | /system/dept | ✅ 3 个部门 | ❌ 不可访问 |
| 操作日志 | /system/log | ✅ 93 条记录，7 页 | ❌ 不可访问 |

---

## 后端 API 验证结果

| 模块 | 接口 | 测试结果 |
|------|------|---------|
| 认证 | POST /auth/login | ✅ 正常返回 JWT |
| 认证 | GET /auth/info | ✅ 返回角色/权限/菜单 |
| 仪表盘 | GET /dashboard/summary | ✅ 返回统计卡片数据 |
| 仪表盘 | GET /dashboard/my-tasks | ✅ 返回当前用户待办任务 |
| 仪表盘 | GET /dashboard/my-bugs | ✅ 返回当前用户待处理 Bug |
| 产品 | 列表/详情/创建/编辑/删除 | ✅ 全部正常 |
| 需求 | 列表/详情/创建/编辑/删除/状态流转/拆分任务 | ✅ 全部正常 |
| 项目 | 列表/详情/创建/编辑/启动/关闭/成员管理 | ✅ 全部正常 |
| 迭代 | 创建/编辑/状态流转/关联需求 | ✅ 全部正常 |
| 任务 | 列表/详情/创建/编辑/状态流转/指派/拖拽/工时 | ✅ 全部正常 |
| Bug | 列表/详情/创建/编辑/状态流转/指派/批量操作 | ✅ 全部正常 |
| 统计 | 燃尽图/Bug分布/任务分布 | ✅ 全部正常 |
| 搜索 | 关键词搜索 | ✅ 返回需求/任务/Bug 结果 |
| 日志 | 操作日志列表/时间线 | ✅ 正常 |
| Bug状态机 | active→resolve→close→reopen | ✅ 全链路正常 |
| 工时汇总 | 按成员统计 | ✅ 正常 |

---

## 结论

**系统整体运行稳定，核心功能完整可用。** 

- 最紧急问题：**角色权限种子缺失**（P1），导致非 admin 用户无法使用系统
- 次要问题：**Bug 状态流转 NPE**（P2），建议增加参数校验
- 其余均为低优先级优化项

建议优先修复角色权限种子问题，这样才能让不同角色的用户正常使用系统功能。