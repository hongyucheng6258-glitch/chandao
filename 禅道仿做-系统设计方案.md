# 基于禅道（ZenTao）的敏捷项目管理系统设计方案

> 技术栈：Spring Boot 3 + Vue 3 + MySQL 8 + Redis 7
> 目录结构：后端 `backend/`，前端 `frontend/`
> 参考项目：https://github.com/easysoft/zentaopms （PHP 开源项目，99 个模块，本方案做合理裁剪）

---

## 一、禅道项目分析（我们要学什么、砍什么）

### 1.1 禅道的核心设计思想

禅道不是简单的"任务看板"，它的精髓是**三条业务主线 + 一套工作项流转体系**：

| 概念 | 说明 | 负责角色 |
|------|------|----------|
| **产品（Product）** | 管"做什么"：需求池（Story）、计划（Plan）、发布（Release） | 产品经理 |
| **项目/执行（Project / Execution）** | 管"怎么做"：迭代（Sprint）、任务（Task）、燃尽图 | 项目经理/研发 |
| **质量（QA）** | 管"做得对不对"：Bug、用例（TestCase）、测试单（TestTask） | 测试 |

三层关系：**产品需求（Story）→ 关联到项目/迭代 → 拆成任务（Task）→ 开发完成 → 提测产生 Bug → Bug 修复后关联发布（Release）**。这条闭环是整个系统的骨架。

### 1.2 禅道架构的可借鉴点与舍弃点

| 禅道做法 | 我们的取舍 |
|----------|-----------|
| 99 个模块，大而全（含 OA、文档、DevOps、AI 等） | **裁剪到约 15 个核心模块**，保住主线闭环 |
| PHP 自研 MVC（control/model/zen/tao 分层） | 换成 Spring Boot 3 标准分层（Controller/Service/Mapper） |
| 自研 DAO + MySQL，约 200 张表 | MyBatis-Plus + 精简到 **30~40 张核心表** |
| 自研权限（group + priv 表，按模块-方法授权） | 换成标准 **RBAC**（用户-角色-权限）+ Spring Security |
| 服务端渲染 + jQuery/ZUI | Vue 3 前后端分离，REST API |
| Scrum / 瀑布 / 看板三种模式 | 毕设规模**只做 Scrum**（迭代/Sprint 模型），架构上留扩展位 |
| 文件缓存、session 文件存储 | Redis 统一缓存 + Token 会话 |

### 1.3 裁剪后的功能范围（MVP → 完整版）

**P0 核心闭环（必须做）：**
1. 用户与权限：登录注册、RBAC、团队/成员管理
2. 产品管理：产品 CRUD、需求（Story）管理、需求优先级/状态流转
3. 项目与迭代：项目 CRUD、迭代（Sprint）管理
4. 任务管理：任务领取、状态流转（未开始/进行中/已完成/已关闭）、工时登记
5. Bug 管理：提 Bug、指派、解决/关闭、与需求/任务关联
6. 工作台（我的地盘）：我的任务、我的 Bug、我的需求
7. 基础统计：项目燃尽图、Bug 分布统计、个人工作量统计

**P1 加分项（时间够再做）：**
8. 测试管理：测试用例库、用例与 Bug 关联
9. 看板视图：任务拖拽看板（列 = 状态）
10. 消息通知：任务指派/Bug 指派站内通知（Redis Pub/Sub 或 WebSocket）
11. 操作日志/动态（Action 表，禅道特色：所有变更记录可追溯）

**P2 明确不做：** 瀑布模型、文档管理、DevOps 集成、AI、移动端。

---

## 二、总体架构设计

### 2.1 技术选型

| 层 | 技术 | 版本 | 选型理由 |
|----|------|------|----------|
| 后端框架 | Spring Boot | 3.3.x | 主流、生态全，毕设答辩好讲 |
| ORM | MyBatis-Plus | 3.5.x | CRUD 零 XML，条件构造器适合复杂筛选（禅道式列表页筛选非常多） |
| 安全 | Spring Security + JWT | — | 无状态认证，配 Redis 存 token 可做单点踢人 |
| 数据库 | MySQL | 8.0 | InnoDB，utf8mb4 |
| 缓存 | Redis | 7.x | 会话/热点数据/分布式锁/统计计数 |
| 接口文档 | SpringDoc OpenAPI | 2.x | 自动生成 Swagger UI，省得手写 API 文档 |
| 工具 | Lombok / MapStruct / Hutool | — | 少写样板代码 |
| 前端框架 | Vue 3 + Vite 5 | — | Composition API + `<script setup>` |
| UI 组件 | Element Plus | 2.x | 后台管理系统标配，表格/表单/树组件全 |
| 状态管理 | Pinia | 2.x | 官方推荐 |
| 路由 | Vue Router | 4.x | 动态路由按权限生成菜单 |
| HTTP | Axios | — | 拦截器统一带 token、统一错误处理 |
| 图表 | ECharts | 5.x | 燃尽图、Bug 统计图 |

### 2.2 后端分层与工程结构（backend/）

采用**按业务域分包**（对应禅道的 module 思想，但用 Spring 风格表达）：

```
backend/
├── pom.xml
└── src/main/
    ├── java/com/pms/
    │   ├── PmsApplication.java
    │   ├── common/                    # 通用层
    │   │   ├── config/                #   Security/Redis/MyBatisPlus/Swagger/CORS 配置
    │   │   ├── exception/             #   全局异常处理 @RestControllerAdvice
    │   │   ├── result/                #   统一返回体 Result<T>、分页 PageResult
    │   │   ├── enums/                 #   状态枚举（任务状态/Bug状态/优先级…）
    │   │   ├── annotation/            #   @LogOperation 操作日志注解
    │   │   └── util/
    │   ├── modules/
    │   │   ├── system/                # 系统域：用户/角色/权限/部门/登录
    │   │   │   ├── controller/
    │   │   │   ├── service/  (impl/)
    │   │   │   ├── mapper/
    │   │   │   ├── entity/
    │   │   │   └── dto/  vo/
    │   │   ├── product/               # 产品域：产品/需求/产品计划
    │   │   ├── project/               # 项目域：项目/迭代(Sprint)/成员
    │   │   ├── task/                  # 任务域
    │   │   ├── bug/                   # Bug域
    │   │   ├── test/                  # 测试域：用例（P1）
    │   │   ├── stats/                 # 统计域：燃尽图/报表
    │   │   └── notice/                # 通知域（P1）
    │   └── framework/                 # 拦截器、过滤器（JWT filter）、AOP
    └── resources/
        ├── application.yml
        ├── application-dev.yml
        └── mapper/                    # 复杂 SQL 的 XML
```

**关键设计决策：**

- **统一返回体**：`{ code, message, data }`，前端 Axios 拦截器统一处理 code ≠ 200 的情况。
- **状态流转用枚举 + 状态机校验**：禅道里 story/task/bug 的状态流转规则很多（如 Bug：激活→已解决→已关闭/重新激活）。在 Service 层写 `checkTransition(from, to)`，不允许非法跳转，这是答辩时的一个亮点。
- **软删除**：所有业务表 `deleted` 字段 + MyBatis-Plus 逻辑删除。
- **审计字段**：`created_by / created_time / updated_by / updated_time` 统一 MetaObjectHandler 填充。
- **操作日志**：自定义注解 `@LogOperation("指派任务")` + AOP 落库 `sys_action_log` 表，对应禅道的 action 模块。

### 2.3 前端工程结构（frontend/）

```
frontend/
├── package.json / vite.config.ts
└── src/
    ├── api/                  # 按域组织的 API 封装（product.ts / task.ts ...）
    ├── assets/
    ├── components/           # 通用组件：StatusTag、PriorityTag、UserSelect、RichEditor
    ├── layout/               # 主布局：侧边栏 + 顶栏 + 标签页
    ├── router/               # 静态路由 + 动态权限路由
    ├── store/                # Pinia：user / permission / tabs
    ├── utils/                # request.ts(axios封装)、权限指令 v-perm
    └── views/
        ├── login/
        ├── dashboard/        # 工作台：我的任务/Bug/需求卡片
        ├── product/          # 产品列表、需求列表、需求详情(含关联任务/Bug)
        ├── project/          # 项目列表、迭代管理、迭代详情(任务墙)
        ├── task/             # 任务列表、任务看板(P1)
        ├── bug/              # Bug 列表、提单页、处理页
        ├── test/             # 用例库(P1)
        ├── stats/            # 燃尽图、报表
        └── system/           # 用户/角色/权限/部门管理
```

---

## 三、数据库设计（核心表，约 30 张）

### 3.1 ER 主线

```
sys_user ──┬──< project_member >──┬── project ──< sprint
           │                      │
product ──< product_plan          └──< task >── (关联) story
   └──< story ───────────────────────────┘
             └──< bug >── testcase(P1)
release（产品发布，关联 story/bug 清单）
```

### 3.2 核心表清单

**系统域（RBAC 五件套 + 日志）：**
| 表 | 说明 |
|----|------|
| `sys_user` | 用户：账号/密码(BCrypt)/姓名/头像/部门/状态 |
| `sys_role` | 角色：管理员/产品经理/项目经理/开发/测试/访客 |
| `sys_permission` | 权限点：菜单+按钮级（如 `task:assign`） |
| `sys_user_role` / `sys_role_permission` | 关联表 |
| `sys_dept` | 部门树 |
| `sys_action_log` | 操作日志/动态（对象类型+对象ID+动作+变更明细 JSON） |
| `sys_notice` | 站内通知（P1） |

**业务域：**
| 表 | 说明 | 关键字段 |
|----|------|----------|
| `product` | 产品 | name/code/负责人/状态 |
| `product_plan` | 产品计划 | 起止日期/目标 |
| `story` | 需求 | 标题/优先级(1-4)/预估工时/状态(draft→active→changed→closed)/所属产品/计划 |
| `project` | 项目 | 名称/起止日期/负责人/状态/关联产品 |
| `sprint` | 迭代 | 所属项目/起止日期/目标/状态 |
| `project_member` | 项目成员 | 角色/可用工时 |
| `task` | 任务 | 所属迭代/关联 story/指派给/预估-已消耗-剩余工时/状态/截止日期 |
| `bug` | 缺陷 | 标题/严重程度/优先级/重现步骤/关联 story、task、迭代/状态(active→resolved→closed)/解决人/解决方案 |
| `test_case` | 用例（P1） | 前置条件/步骤(JSON)/关联 story |
| `release` | 发布 | 版本号/日期/关联完成的需求与修复的 Bug |

**设计要点（对应禅道经验）：**
1. **工作项统一三态字段**：`status`（业务状态）+ `assigned_to`（当前处理人）+ `closed_reason`（关闭原因），查询"我的待办"时一个索引搞定。
2. **关联关系用显式外键列而非关联大表**：`task.story_id`、`bug.story_id`、`bug.task_id`，比禅道的 relation 通用表更直观，毕设规模够用。
3. **工时三字段**：`estimate / consumed / left`，燃尽图直接按日期聚合 `task` 的变更快照（见 3.3）。
4. **所有列表页字段建联合索引**：如 `task(sprint_id, status, assigned_to)`——禅道列表页多条件筛选是常态，索引设计可以在论文里单独写一节。

### 3.3 燃尽图数据来源

新增 `task_snapshot`（或 `sprint_burndown`）表：每天定时任务（Spring `@Scheduled`）记录每个迭代的「剩余总工时」。比实时计算快得多，也体现 Redis+定时任务+报表的组合设计。

---

## 四、Redis 使用设计（不是摆设，每个点都能在答辩讲）

| 场景 | 数据类型 | Key 设计 | 说明 |
|------|----------|----------|------|
| 登录 Token | String | `login:token:{userId}` | JWT + Redis 双校验，支持强制下线 |
| 用户权限缓存 | Hash | `perm:user:{userId}` | 登录时加载，改权限后主动失效 |
| 热点数据缓存 | String(JSON) | `product:list:{userId}` 等 | 产品/项目下拉列表，变更时删除缓存（Cache-Aside） |
| 接口防重复提交 | String | `repeat:{userId}:{uri}:{md5}` | SETNX + 过期时间，防双击建重复任务 |
| 分布式锁 | String | `lock:task:assign:{id}` | 任务并发领取防超派（SET NX PX + Lua 解锁） |
| 统计计数 | Hash | `stats:project:{id}` | 任务数/Bug 数实时计数，列表页不用 count(*) |
| 消息通知 | Pub/Sub 或 Stream | `notice:channel` | P1：指派实时推送（配合 WebSocket） |
| 燃尽图缓存 | String | `burndown:{sprintId}:{date}` | 报表接口响应提速 |

---

## 五、核心业务流程设计（论文里的"系统流程图"素材）

**需求到发布的完整闭环：**
1. 产品经理在产品下建 **Story**（状态 draft）→ 评审后激活（active）
2. 项目经理建项目 + **Sprint**，把 Story 拉入迭代
3. 开发把 Story 拆成 **Task**（可多级拆分），领取任务，状态：wait→doing→done
4. 任务全部 done → 迭代提测
5. 测试提 **Bug** 关联 Story/迭代 → 指派开发 → 解决(resolved，选解决方案) → 测试验证关闭(closed) 或打回重新激活
6. Sprint 结束 → 建 **Release**，自动汇总"本次完成的需求清单 + 修复的 Bug 清单"
7. 全程 `sys_action_log` 记录动态，工作台可见

**状态机示例（Bug）**：`active →(解决) resolved →(验证通过) closed`；`resolved →(验证不通过) active`；`active →(确认非Bug/重复) closed(带关闭原因)`。

---

## 六、API 设计规范（示例）

REST 风格，统一前缀 `/api/v1`：

```
POST   /api/v1/auth/login                 登录
GET    /api/v1/stories?productId=&status=&page=   需求分页列表
POST   /api/v1/stories                    新建需求
PUT    /api/v1/stories/{id}/status        需求状态流转（body: {action: "activate"}）
POST   /api/v1/tasks                      拆任务
PUT    /api/v1/tasks/{id}/assign          指派
PUT    /api/v1/tasks/{id}/hours           登记工时 {consumed, left}
GET    /api/v1/sprints/{id}/burndown      燃尽图数据
GET    /api/v1/dashboard/my-todo          我的待办（任务+Bug+需求聚合）
```

错误码分段：`1xxx` 参数/通用，`2xxx` 权限，`3xxx` 业务状态冲突（如非法状态流转）。

---

## 七、开发计划（建议 8 周）

| 周 | 内容 | 产出 |
|----|------|------|
| W1 | 需求细化、ER 图定稿、建库脚本、脚手架搭建（后端 SpringBoot 工程 + 前端 Vite 工程跑通登录页） | 可运行的空架子 |
| W2 | 系统域：登录 JWT、RBAC、用户/角色/部门管理 + 前端动态菜单 | 权限体系完成 |
| W3 | 产品域：产品/计划/Story CRUD + 状态流转 | 产品线可用 |
| W4 | 项目域：项目/Sprint/成员 + Story 拉入迭代 | 项目线可用 |
| W5 | 任务域：任务 CRUD/指派/工时 + Bug 域 CRUD/流转 | **核心闭环打通** |
| W6 | 工作台聚合、操作日志、Redis 各场景落地、燃尽图定时任务 | 亮点功能完成 |
| W7 | 统计报表（ECharts）、看板拖拽、站内通知（P1 选做）、联调修 Bug | 完整版 |
| W8 | 测试（Postman 用例 + 简单压测）、部署（Docker Compose）、写论文 | 交付 |

**部署**：`docker-compose.yml` 一把起 MySQL + Redis + 后端 Jar + Nginx(前端静态资源)，答辩演示方便。

---

## 八、毕设亮点（写论文/答辩的加分点）

1. **状态机驱动的缺陷生命周期管理**——比"增删改查系统"高一个档次
2. **RBAC + JWT + Redis 的企业级权限体系**（含按钮级权限、强制下线）
3. **Redis 深度应用**：缓存/分布式锁/防重/计数/消息，而不是只存个 token
4. **燃尽图 = 定时任务 + 数据快照 + 聚合报表**，有数据仓库的雏形思想
5. **操作日志 AOP + 变更明细追踪**，可追溯性设计（禅道 action 模块的现代化实现）
6. 对比分析：论文绪论里可以直接对比禅道（PHP 单体、服务端渲染）与本系统（前后端分离、缓存体系）的架构演进

---

## 九、风险与注意事项

- **范围控制**：禅道有 200+ 表、99 模块，千万别想着全抄。守住第 1.3 节的裁剪范围，P0 做完就够用。
- **License**：禅道是 LGPL/ZPL 开源协议，我们是**参考其业务设计自研重写**，不复制代码，无版权问题。
- **先闭环后美化**：W5 之前忍住别调样式，闭环通了项目就死不了。
